import { createTheme } from "@mui/system";

const theme = createTheme({
  palette: {
    primary: {
      main: "#333",
      light: "#909090",
      lightgray: "#c7c7c7",
      white: "#fff",
      black: "#000",
    },
  },
});

export default theme;
